package com.complexas.repo;

import com.complexas.model.Enrich;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EnrichRepository extends JpaRepository<Enrich, Integer> {
}
